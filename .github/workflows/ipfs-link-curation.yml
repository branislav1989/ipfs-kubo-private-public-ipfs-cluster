name: IPFS Link Curation

on:
  schedule:
    - cron: '0 2 * * *'  # 2:00 AM UTC daily
  workflow_dispatch:

jobs:
  curate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests feedparser beautifulsoup4
      
      - name: Run IPFS Link Curator
        run: python3 ipfs-hosting/links/ipfs-link-curator.py
      
      - name: Verify output
        run: |
          if [ -f ipfs-hosting/links/ipfs_ecosystem_links.json ]; then
            echo "✅ IPFS ecosystem links generated"
            ls -lh ipfs-hosting/links/ipfs_ecosystem_links.json
          else
            echo "❌ Output file not found"
            exit 1
          fi
