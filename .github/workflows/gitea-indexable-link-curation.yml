name: Daily Gitea Indexable Link Curation

on:
  schedule:
    - cron: '30 2 * * *'  # Runs daily at 2:30 AM UTC
  workflow_dispatch:

jobs:
  gitea-indexable-curation:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Run Gitea indexable link curator
        run: |
          python3 gitea-hosting/links/gitea-link-curator.py
      
      - name: Verify output
        run: |
          echo "✅ Gitea indexable link curation completed"
          ls -lh gitea-hosting/links/*.json 2>/dev/null | tail -3 || echo "Output files ready"
